const Header = () => {
	const template = `
        <header>
            <h1>Simple Workflow</h1>
            <p>A vanilla JS built workflow platform.</p>
        </header>
    `;

	return template;
};

export default Header;
